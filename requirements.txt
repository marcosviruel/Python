# dependências do pipeline
requests==2.31.0
python-dotenv==1.0.0

# dependências de teste
pytest==8.1.0
pytest-mock==3.12.0

# tipagem (opcional mas recomendado)
mypy==1.9.0
responses==0.25.0